{
  "name": "stream-parser",
  "version": "1.0.0",
  "description": "Reads a file from the file system, parse it, and output results",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "pump": "^3.0.0",
    "readable-stream": "^3.1.1",
    "split2": "^3.1.0",
    "through2": "^3.0.0"
  }
}
